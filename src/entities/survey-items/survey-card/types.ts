export type SurveyCardProps = {
    id: number;
    title: string;
    navigationUrl: string;
    deadlineFrom: string;
    deadlineTo: string;
    totalParticipants: number;
    completedParticipants: number;
    status: string;
};

