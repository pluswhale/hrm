export type PracticeCardProps = {
    navUrl: string;
    id: number;
    name: string;
    created_at: string;
    deadline: string;
    is_active: boolean;
    desired_count_candidates: string;
};

